services:
  pagif:
    container_name: pagif
    image: ifrs/pagif:latest
    build: .
    restart: unless-stopped
    user: "node"
    environment:
      - NODE_ENV=production
      - NUXT_HOST=0.0.0.0
      - NUXT_PORT=3000
    expose:
      - "3000"
    command: "nuxt start"
