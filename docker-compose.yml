services:
  pagif:
    container_name: pagif
    image: ifrs/pagif:latest
    build: .
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - NUXT_HOST=${NUXT_HOST}
      - NUXT_PORT=${NUXT_PORT}
    expose:
      - "${NUXT_PORT}"
