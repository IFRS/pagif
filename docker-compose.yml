services:
  pagif:
    container_name: pagif
    image: ifrs/pagif:latest
    build: .
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - NUXT_HOST=0.0.0.0
      - NUXT_PORT=3000
      # Build
      # - PAGTESOURO_URL=
      # - BROWSER_BASE_URL=
      # - SERVER_BASE_URL=
      # - RECAPTCHA_SITE_KEY=
      # Runtime
      # - DB_URL=
      # - DB_PORT=
      # - DB_USER=
      # - DB_PASSWORD=
      # - DB_NAME=
      # - SESSION_SECRET=
      # - GOOGLE_CLIENT_ID=
      # - GOOGLE_CLIENT_SECRET=
      # - RECAPTCHA_SECRET=
    expose:
      - "3000"
